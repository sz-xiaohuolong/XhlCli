# XhlCLI 产品需求文档

> 文档状态：已确认，可用于后续技术设计与分期实施
> 产品阶段：0→1 规划
> 技术基线：Java 21，终端优先，本地优先
> 文档版本：v1.0
> 更新日期：2026-08-25

## 1. 文档目的

本文档定义 XhlCLI 的产品定位、目标用户、核心体验、功能边界、质量要求和分期交付路线。它回答“为什么做、为谁做、做什么、做到什么程度”，作为后续需求研究、技术设计、AI 开发规则、实现计划和阶段验收的共同依据。

本文档不直接规定类、接口和数据库表结构。进入某一期开发前，必须先基于对应子 PRD 编写该期技术设计，再拆分实现任务。

## 2. 产品概述

XhlCLI 是一个基于 Java 21 的本地智能终端 Coding Agent。开发者可以在项目目录中使用自然语言描述开发任务，由 Agent 在用户授权范围内检索代码、读取文件、规划步骤、修改内容、执行命令、检查结果并汇总变更。

产品以“完成一个可验证的代码任务”为核心，而不是只提供聊天或代码片段生成。它通过可观察的执行过程、分级授权、可回退修改、上下文管理和开放工具协议，让用户始终知道 Agent 正在做什么、为什么这样做，以及如何验证和撤销结果。

## 3. 产品愿景

让 开发者拥有一个可理解、可控制、可扩展的终端编程搭档：既能快速完成真实开发任务，也能作为学习 Agent 架构、工具调用、上下文工程和多智能体协作的完整工程样本。

## 4. 用户问题

### 4.1 主要问题

- 开发者需要在编辑器、终端、搜索工具和浏览器之间频繁切换，简单修改也会产生较高操作成本。
- 通用聊天模型不了解本地仓库，回答容易脱离实际代码结构。
- 一次性生成大量代码缺少过程控制，出现错误后难以定位是哪一步引入。
- 长任务持续堆积对话和工具结果，容易超过上下文预算或让模型忽略关键信息。
- 外部工具接入方式不统一，每增加一种能力都可能侵入 Agent 核心逻辑。
- 自动修改文件和执行命令存在安全风险，用户需要明确的授权、审计和回退能力。

### 4.2 机会

终端天然具备项目上下文、命令执行能力和版本控制环境。通过统一 Agent 循环、工具协议、权限策略与验证闭环，可以把自然语言意图转化为一组透明、可检查、可恢复的工程操作。

## 5. 目标用户

### 5.1 核心用户

- 熟悉 Java 和命令行，希望提高日常开发效率的个人开发者。
- 希望系统学习 Coding Agent 原理和工程实现的学生或初中级工程师。
- 需要在已有仓库中完成检索、修改、测试和 Git 辅助操作的开源项目维护者。

### 5.2 次要用户

- 希望通过 MCP、Skill 或自定义工具扩展 Agent 能力的高级开发者。
- 需要在自动化流程中调用 Agent 任务能力的平台开发者。

### 5.3 暂不服务的用户

- 需要企业级多人权限、集中计费和审计后台的大型组织。
- 需要完全无人值守处理生产环境高风险操作的运维团队。
- 只需要网页聊天、不使用本地终端或代码仓库的普通消费者。

## 6. 产品目标与成功标准

### 6.1 产品目标

1. 在项目目录内通过自然语言完成“理解—修改—验证—总结”的开发闭环。
2. 让每次模型决策、工具调用、文件变更和命令执行都可观察、可授权、可追踪。
3. 通过精确搜索、语义检索和分层记忆，在有限上下文内理解中大型代码库。
4. 通过标准化扩展机制接入外部工具，同时保持 Agent 核心与工具实现解耦。
5. 形成阶段清晰、测试充分、文档完整、可持续演进的开源工程。

### 6.2 北极星指标

在内置基准任务集中，用户无需手工编辑代码，Agent 即可完成任务并通过自动验收的比例。

### 6.3 辅助指标

- 首次成功完成一个本地代码任务的中位耗时。
- 工具调用成功率与失败后可恢复率。
- 任务结束时自动验证执行率。
- 高风险操作未经明确授权的执行次数，目标恒为 0。
- 上下文压缩后任务目标与未完成事项的保留率。
- 阶段回归测试通过率。
- 新用户从下载到完成首个任务所需步骤数。

## 7. 非目标

以下内容不属于 0→1 主路线：

- 自研大语言模型或 Embedding 模型。
- 替代完整 IDE、代码托管平台或 CI/CD 平台。
- 默认修改项目根目录以外的文件。
- 默认无人值守执行删除、发布、付款、凭据管理等高风险操作。
- 第一版支持企业级租户、RBAC、集中账单或云端控制台。
- 第一版支持音频、视频、图像生成或远程桌面控制。
- 为追求功能数量而一次性接入大量模型和工具。

## 8. 产品原则

### 8.1 编码闭环优先

每项能力必须服务于真实代码任务。优先保证从用户意图到可验证结果的完整链路，再扩充模式和工具数量。

### 8.2 安全默认值

读取与检索可以低摩擦执行；写文件、执行命令、连接外部服务和恢复快照必须经过策略判断。用户批准不能绕过系统硬限制。

### 8.3 过程可观察

产品应展示当前阶段、计划、工具名称、关键参数摘要、耗时、结果和失败原因，但不要求展示模型私有思维过程。

### 8.4 本地优先

项目文件、索引、会话、记忆、审计日志和快照默认保存在本机。发送给模型或外部服务的数据必须受上下文范围和脱敏规则约束。

### 8.5 小步可回退

长任务应拆成可验证的小步骤。每次文件修改都必须可查看差异，重要任务必须支持恢复到修改前状态。

### 8.6 精确检索优先

文件查找、文本搜索和按范围读取是代码定位的第一选择；语义 RAG 用于意图模糊、跨文件关联或精确搜索无结果的场景。

### 8.7 文档驱动演进

每一期遵循 Research → PRD → Tech Design → AGENTS.md → Build 的顺序。需求、设计、测试、演示和变更记录与代码同步提交。

## 9. 核心用户场景

### 场景 A：理解陌生代码

用户询问某个功能在哪里实现。Agent 先搜索符号和调用关系，再按需读取文件，最后给出带路径和代码位置的解释，不修改仓库。

### 场景 B：完成小型功能

用户描述新增字段或接口。Agent 确认影响范围，修改必要文件，执行相关测试，并汇总改动和验证结果。

### 场景 C：修复缺陷

用户提供错误现象或日志。Agent 形成假设、定位相关代码、复现失败、实施最小修改并运行回归测试。

### 场景 D：处理复杂任务

用户提出跨模块需求。Agent 生成带依赖关系的计划，等待用户确认后按阶段执行；独立步骤可以并行，失败步骤可重试或重新规划。

### 场景 E：扩展外部能力

用户配置 MCP Server 或 Skill。XhlCLI 发现可用工具和资源，将其按统一格式暴露给 Agent，并继续执行权限检查和审计。

## 10. 核心体验闭环

```text
进入项目目录
  → 输入自然语言任务
  → Agent 判断是否需要澄清或制定计划
  → 检索并读取最少必要上下文
  → 提出或执行工具调用
  → 权限层审核高风险操作
  → 修改文件或运行命令
  → 读取诊断、测试和差异
  → 必要时修正
  → 输出结果、验证证据和剩余风险
```

任务完成必须同时满足：用户目标已处理、必要验证已执行、失败项已说明、变更范围可追踪。模型仅输出“已经完成”不能作为完成依据。

## 11. 功能架构

### 11.1 交互层

- 启动、配置和项目识别。
- 流式对话与多行输入。
- 斜杠命令、路径引用和命令补全。
- 状态、计划、工具调用、差异与验收结果展示。

### 11.2 Agent 执行层

- ReAct 单 Agent 循环。
- Plan-and-Execute 与依赖图调度。
- 并行工具执行和取消。
- Multi-Agent 角色协作与结果汇总。

### 11.3 工具与权限层

- 文件读取、目录浏览、精确搜索、文件修改和命令执行。
- 工具注册、参数校验、结果预算和统一错误模型。
- 路径围栏、命令策略、人工审批和操作审计。
- MCP 动态工具、资源和提示模板扩展。

### 11.4 上下文与知识层

- 会话历史、任务状态、项目规则和长期记忆。
- Token 预算、裁剪、摘要与检索。
- 代码切分、增量索引、向量检索和上下文组装。
- Prompt 分层与按需 Skill 注入。

### 11.5 工程闭环层

- Git 差异查看与辅助操作。
- 修改前后快照及恢复。
- 语法或 LSP 诊断。
- 测试命令执行、结果解析和验收总结。
- 后台任务、事件流和 Runtime API。

## 12. 功能优先级

### P0：必须形成的产品核心

- Java 21 CLI 启动与流式模型对话。
- ReAct 循环及明确的停止条件。
- 文件、目录、精确搜索、写入和命令工具。
- 项目根路径围栏、高风险操作审批和审计。
- 代码差异展示、测试执行和任务结果总结。
- 上下文预算、会话清理和基本项目记忆。
- 可重复运行的核心测试与示例任务。

### P1：增强复杂任务能力

- 增量代码索引和语义检索。
- Plan-and-Execute、DAG 和并行执行。
- Multi-Agent 角色协作。
- 多模型适配与运行时切换。
- MCP、联网搜索、网页读取和浏览器工具。
- Skill 与分层 Prompt。
- 产品化终端交互。

### P2：工程化与差异化能力

- LSP 诊断和隔离快照。
- 后台任务与 Runtime API。
- 图片上下文。
- 执行事件回放和基准评测。
- 插件开发者文档、跨平台发行和开源治理。

## 13. 全局功能要求

### 13.1 任务生命周期

每次任务至少包含 `created`、`running`、`waiting_for_approval`、`verifying`、`completed`、`failed`、`canceled` 中的适用状态。状态切换应可记录，并向终端展示用户需要了解的部分。

### 13.2 工具调用

- 工具必须提供稳定名称、用途说明、JSON 参数结构和结构化结果。
- 参数校验失败不得进入实际执行。
- 工具结果必须区分成功、业务失败、权限拒绝、超时和取消。
- 大结果应截断并提示继续读取方式，不能无上限进入模型上下文。
- 同一轮多个只读或互不冲突工具可以并行，但结果顺序必须稳定。

### 13.3 文件操作

- 默认工作范围是启动时识别的项目根目录。
- 读取应支持行范围；写入前应确认目标路径和预期变更。
- 不允许通过 `..`、绝对路径或符号链接逃逸项目根。
- 修改后应提供统一差异，并允许继续验证或恢复。

### 13.4 命令执行

- 命令必须在明确工作目录和超时时间内执行。
- 高风险命令必须请求批准；系统拒绝规则优先于用户批准。
- 输出必须限制大小并保留退出码、耗时、标准输出和错误摘要。
- 不得在日志和模型上下文中明文暴露已识别的密钥。

### 13.5 上下文管理

- 只注入完成当前任务所需的最小上下文。
- 项目稳定规则、当前任务状态、会话历史和长期记忆必须分层管理。
- 逼近模型上下文窗口前应预留响应和工具结果空间。
- 压缩后必须保留用户目标、已完成步骤、未完成事项、关键决策和工具边界。
- 用户可以查看、搜索、删除和清空持久记忆。

### 13.6 验证与完成判定

- 文件修改后至少进行适用的语法、构建、测试或静态检查之一。
- 验证命令失败时不得宣称任务成功。
- 无法运行验证时必须说明原因、未验证范围和人工验证方法。
- 最终总结包括修改内容、涉及文件、验证证据、风险与建议下一步。

## 14. 安全、隐私与合规要求

- API Key 仅从环境变量或本地配置读取，不写入仓库、日志和导出会话。
- 审计记录对 token、password、secret、authorization 等字段脱敏。
- 网络访问默认拒绝本机回环、私网地址和非 HTTP(S) 协议，明确的本地开发场景除外。
- 外部 MCP 与 Skill 不自动获得高于内置工具的权限。
- 恢复、删除、覆盖和发布类操作必须展示影响范围。
- 项目数据默认不上传到 XhlCLI 自建服务；调用第三方模型时应在文档中说明数据流向。
- 自动化模式不得绕过路径围栏、凭据保护和系统级拒绝策略。

## 15. 非功能需求

### 15.1 兼容性

- 运行时要求 Java 21。
- 首要支持 macOS 和 Linux，Windows 通过 PowerShell 或 WSL 验证。
- 在不支持 ANSI 的终端提供纯文本降级。

### 15.2 性能

- 冷启动到出现输入提示，P0 阶段目标不超过 3 秒，不包含外部服务初始化。
- 普通本地文件搜索应在 1 秒内开始返回结果。
- 外部服务启动不得无限阻塞终端，必须有超时和状态提示。
- 索引过程应支持增量更新，未变化文件不得重复处理。

### 15.3 可靠性

- 单个工具失败不能破坏完整会话记录。
- 取消后不得继续执行新的写入或命令工具。
- 异常退出后不能留下损坏的配置、索引或任务状态。
- 所有持久化格式必须有版本字段或兼容策略。

### 15.4 可测试性

- 核心 Agent 循环、权限策略、工具参数和上下文裁剪必须可脱离真实模型测试。
- 网络、模型、MCP 和命令执行使用可替换接口及测试替身。
- 每一期包含单元测试、必要的集成测试和可复制的验收证据。手工演示场景仅在对应子 PRD 或阶段 DoD 要求且未明确豁免时需要。
- 关键代码任务形成确定性基准集，避免仅凭演示判断效果。

### 15.5 可维护性

- Agent、模型、工具、权限、存储和渲染模块保持单向依赖。
- 新增模型不应修改 Agent 主循环；新增工具不应修改模型客户端。
- 产品需求变更必须同步 PRD；架构决策变更必须同步技术设计或 ADR。
- 避免为未知未来需求提前引入复杂抽象。

## 16. 阶段路线图

| 阶段 | 名称 | 主要交付 | 依赖 | 优先级 |
|---|---|---|---|---|
| 00 | 项目基线 | 文档体系、仓库规范、可运行骨架 | 无 | P0 |
| 01 | 终端对话 | Java 21 CLI、配置、流式问答 | 00 | P0 |
| 02 | ReAct Agent | 决策循环、工具调用协议、停止控制 | 01 | P0 |
| 03 | 本地编码工具 | 文件、目录、搜索、写入、命令闭环 | 02 | P0 |
| 04 | 安全与审批 | 路径围栏、命令策略、HITL、审计 | 03 | P0 |
| 05 | 上下文与记忆 | 分层上下文、预算、压缩、持久记忆 | 02–04 | P0 |
| 06 | 代码精确检索 | glob、grep、范围读取、结果预算 | 03、05 | P0 |
| 07 | 代码库 RAG | 切分、增量索引、向量检索、组装 | 05、06 | P1 |
| 08 | 计划执行 | 计划确认、任务依赖、失败重规划 | 03–07 | P1 |
| 09 | 并行执行 | 独立工具和任务的有界并行、取消 | 08 | P1 |
| 10 | Multi-Agent | 角色分工、上下文交接、审查闭环 | 08、09 | P1 |
| 11 | 多模型适配 | Provider 抽象、能力声明、运行时切换 | 01、05 | P1 |
| 12 | MCP 扩展 | 工具发现、调用、资源、生命周期 | 04、11 | P1 |
| 13 | Web 与浏览器 | 搜索、正文获取、浏览器会话与策略 | 04、12 | P1 |
| 14 | Skill 与 Prompt | 分层提示词、按需技能加载、覆盖机制 | 05、12 | P1 |
| 15 | 终端产品化 | 行内 TUI、状态区、补全、高亮、历史 | 01–14 | P1 |
| 16 | 诊断与快照 | LSP/语法诊断、Side-History、恢复 | 03、04、15 | P2 |
| 17 | Runtime 与多模态 | 后台任务、事件 API、图片上下文 | 09、11、15、16 | P2 |
| 18 | 开源发行 | 基准评测、发行包、贡献指南、演示资产 | 00–17 | P2 |

阶段编号代表产品依赖和推荐开发顺序，不要求一次性承诺全部后续范围。每一期只有在验收条件满足、文档同步、验证证据可复现时才能进入已交付状态。

## 17. 里程碑门禁

### M0：可运行基础（Phase 00–01）

用户可以安装、配置并在项目目录内与模型进行稳定流式对话。

### M1：可用 Coding Agent（Phase 02–06）

用户可以让 Agent 安全完成小型代码检索、修改和验证任务。这是首个公开 MVP。

### M2：复杂任务智能（Phase 07–10）

Agent 可以理解较大仓库、规划跨模块任务、并行执行独立步骤并进行角色化审查。

### M3：开放生态与产品体验（Phase 11–15）

用户可以切换模型、接入 MCP 和 Skill，并获得稳定、清晰的终端交互体验。

### M4：工程化发行（Phase 16–18）

产品具备诊断、恢复、后台任务、多模态输入、基准评测和跨平台发行能力。

## 18. 每期标准工作流

每一期均执行以下流程：

1. **Research**：确认用户问题、参考方案、依赖和风险，更新研究记录。
2. **PRD**：确认本期范围、非目标、交互、异常场景和验收标准。
3. **Tech Design**：确定模块边界、接口、数据模型、技术选型和测试策略。
4. **AGENTS.md**：补充当前仓库结构、开发规范、验证命令和禁止事项。
5. **Build**：按可测试的小任务实现，先跑通主路径，再处理边界和体验。
6. **Verify**：执行自动测试并保存可复现证据；手工场景仅在对应子 PRD 或阶段 DoD 要求且未明确豁免时执行。
7. **Release**：同步 README 和 CHANGELOG；演示素材与版本标签仅在对应子 PRD 或阶段 DoD 要求且未明确豁免时需要。

阶段开发中必须限定修改范围。每个独立功能通过验证后再提交，避免将多个无法单独验收的功能压入同一提交。

## 19. GitHub 阶段交付规范

每一期至少包含：

- 对应子 PRD 和技术设计文档。
- 与需求关联的源代码和自动测试。
- README 中的阶段能力、快速开始和使用示例。
- 一份可复制执行的验收命令清单。
- 对应子 PRD 或阶段 DoD 要求且未明确豁免时，提供真实终端录屏、GIF 或截图，敏感信息必须脱敏。
- CHANGELOG 条目和语义明确的提交记录。
- 已知限制与下一阶段边界，不夸大尚未交付的能力。

推荐提交粒度：项目骨架、失败测试、最小实现、异常处理、文档演示分别提交。提交信息描述产品变化，不使用“完善一下”“若干修改”等模糊措辞。

自动测试和可复制执行的验收命令/证据始终为阶段门禁。明确豁免只能由对应子 PRD 或阶段 DoD 记录，且只适用于被点名的手工场景、演示素材或版本标签；不能免除自动测试或可复制验收证据。

## 20. 版本与范围管理

- 总 PRD 描述长期产品方向；子 PRD 只描述单期可交付范围。
- 子 PRD 进入“开发中”后，新增需求默认进入后续阶段，除非它阻塞本期验收。
- 任何需求删除、延期或优先级变化必须在子 PRD 的变更记录中说明。
- README 只声明已经通过验收的能力；规划能力保留在 PRD 或 Roadmap。
- 技术实现与 PRD 不一致时，应先确认是实现缺陷还是需求变化，再同步文档。

## 21. 风险与应对

| 风险 | 影响 | 应对策略 |
|---|---|---|
| 过早追求完整功能集 | 主链路长期不可用 | 以 M1 编码闭环作为首个公开 MVP |
| Agent 循环失控 | 成本增加或产生错误修改 | 最大迭代、超时、取消、预算和重复调用检测 |
| 模型差异破坏协议 | 工具调用或历史格式不兼容 | Provider 能力声明与兼容性测试 |
| 上下文无限增长 | 超窗、成本上升、回答漂移 | 分层记忆、结果预算、摘要和按需检索 |
| RAG 结果噪声过高 | 错误理解代码 | 精确搜索优先、增量索引、引用来源和评测集 |
| 高风险操作误执行 | 数据或仓库受损 | 硬策略、HITL、审计、快照和最小权限 |
| 终端渲染与业务耦合 | 难以测试和维护 | Renderer 接口与事件驱动展示 |
| 阶段文档与代码脱节 | GitHub 信息失真 | 每期 DoD 强制同步文档和验证证据 |

## 22. 分期 PRD 索引

- [Phase 00：项目基线](docs/prd/phase-00-project-foundation.md)
- [Phase 01：终端对话](docs/prd/phase-01-terminal-chat.md)
- [Phase 02：ReAct Agent](docs/prd/phase-02-react-agent.md)
- [Phase 03：本地编码工具](docs/prd/phase-03-local-tools.md)
- [Phase 04：安全与审批](docs/prd/phase-04-safety-and-approval.md)
- [Phase 05：上下文与记忆](docs/prd/phase-05-context-and-memory.md)
- [Phase 06：代码精确检索](docs/prd/phase-06-code-search.md)
- [Phase 07：代码库 RAG](docs/prd/phase-07-codebase-rag.md)
- [Phase 08：计划执行](docs/prd/phase-08-plan-and-execute.md)
- [Phase 09：并行执行](docs/prd/phase-09-parallel-execution.md)
- [Phase 10：Multi-Agent](docs/prd/phase-10-multi-agent.md)
- [Phase 11：多模型适配](docs/prd/phase-11-multi-model.md)
- [Phase 12：MCP 扩展](docs/prd/phase-12-mcp.md)
- [Phase 13：Web 与浏览器](docs/prd/phase-13-web-and-browser.md)
- [Phase 14：Skill 与 Prompt](docs/prd/phase-14-skills-and-prompts.md)
- [Phase 15：终端产品化](docs/prd/phase-15-terminal-productization.md)
- [Phase 16：诊断与快照](docs/prd/phase-16-lsp-and-snapshots.md)
- [Phase 17：Runtime 与多模态](docs/prd/phase-17-runtime-and-multimodal.md)
- [Phase 18：开源发行](docs/prd/phase-18-open-source-release.md)

## 23. 术语

- **Run**：从用户提交任务到完成、失败或取消的一次 Agent 执行。
- **Turn**：一次用户输入及其对应的 Agent 响应过程。
- **Tool**：模型可选择调用、由 XhlCLI 执行的结构化能力。
- **HITL**：Human-in-the-Loop，需要用户参与的授权或决策流程。
- **MCP**：用于连接外部工具、资源和提示模板的标准协议。
- **Skill**：按场景加载的可复用操作说明、参考资料与辅助脚本集合。
- **RAG**：从代码索引中检索相关片段并组装到模型上下文的过程。
- **DoD**：Definition of Done，本阶段进入已交付状态必须满足的条件。

## 24. 总体验收条件

长期路线完成时，XhlCLI 应满足：

1. 新用户按照 README 可以在 10 分钟内完成安装、配置和首个示例任务。
2. Agent 能在受控项目目录中完成代码检索、修改、命令执行和验证闭环。
3. 简单任务默认走 ReAct，复杂任务可以显式进入计划或团队模式。
4. 高风险操作均经过策略判断，未经许可不会执行，所有执行均可审计。
5. 大型仓库任务可以组合精确搜索与语义检索，并引用实际文件来源。
6. 外部 MCP、Skill、模型和渲染层可以独立扩展，不需要重写 Agent 主循环。
7. 长任务可以压缩上下文、取消执行、查看状态，并从修改前快照恢复。
8. 核心能力有自动测试、基准任务、演示证据和明确的已知边界。

## 25. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-25 | 确立产品定位、核心闭环、P0/P1/P2 优先级及 Phase 00–18 路线 |
