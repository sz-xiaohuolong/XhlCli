# XhlCLI 需求研究

> 文档状态：已确认
> 研究阶段：产品立项
> 更新日期：2026-08-25

## 1. 研究目的

本文件记录 XhlCLI 立项前的用户问题、产品假设、参考实现分析、实施约束和阶段策略。它为 `PRD.md` 提供决策依据，不替代产品需求或技术设计。

## 2. 初始想法

建设一个基于 Java 21 的终端 Coding Agent，让开发者通过自然语言完成代码检索、文件修改、命令执行、测试验证和 Git 辅助操作；在此基础上继续加入上下文工程、代码库 RAG、计划执行、Multi-Agent、MCP 与终端产品化能力。

项目需要以真实、连续、可运行的阶段演进，而不是一次性出现最终代码。每一期都应形成能够独立解释、运行、测试和演示的工程增量。

## 3. 目标用户研究

### 3.1 核心用户

- 已掌握 Java 基础，希望学习 AI Agent 工程实现的开发者。
- 习惯终端工作流，希望减少重复检索与修改操作的个人开发者。
- 准备 Java、AI 应用或 Agent 方向面试，需要能够深入解释完整项目的人。

### 3.2 用户已有能力

默认核心用户能够：

- 阅读 Java 代码与 Maven 配置。
- 使用 Git 管理本地修改。
- 理解 HTTP API、JSON 和基本并发概念。
- 使用环境变量配置第三方服务。

产品不要求用户预先理解 ReAct、MCP、Embedding 或上下文压缩；这些概念需要通过文档、日志与演示逐步呈现。

## 4. 需要解决的问题

### P-01 本地上下文缺失

通用聊天模型不知道用户仓库的真实目录、符号、构建方式和当前修改。仅粘贴少量代码容易遗漏调用关系，粘贴整个仓库又会浪费上下文。

### P-02 开发闭环割裂

生成代码只是任务的一部分。定位文件、应用变更、执行测试、处理错误和总结差异仍需要用户在多个工具间切换。

### P-03 Agent 过程不可控

模型可能选择错误工具、重复执行、修改过多文件或运行危险命令。缺少停止条件、权限层和审计时，自动化程度越高，风险越大。

### P-04 长任务上下文膨胀

工具结果和多轮历史持续累积，会造成超窗、成本上升或模型遗忘目标。单纯扩大模型窗口不能替代上下文治理。

### P-05 能力扩展耦合

如果每个外部服务都直接写入 Agent 主循环，代码会迅速变得难以测试。工具、模型、协议、策略和渲染需要稳定边界。

### P-06 项目经历缺少可验证过程

一个只有最终代码的仓库很难说明架构为什么演进。阶段化需求、设计、测试、提交和演示能让工程决策可追踪，也迫使维护者真正掌握每个模块。

## 5. 研究发现

### 5.1 完整 Coding Agent 的最小单位是闭环

仅有模型聊天、文件读取或代码生成都不能构成可用 Coding Agent。最小可用产品必须串起：

```text
理解任务 → 找到代码 → 修改文件 → 运行验证 → 汇总证据
```

因此阶段顺序应优先形成纵向闭环，再增加 RAG、Multi-Agent 等横向能力。

### 5.2 精确搜索应早于 RAG

代码中的类名、方法名、配置键和错误文本适合精确搜索。它不需要 Embedding，结果可解释且能引用行号。RAG 更适合作为自然语言和跨模块语义的补充。

### 5.3 安全必须独立于模型

系统不能依赖 Prompt 要求模型“谨慎”。路径围栏、命令策略、审批和审计必须在工具执行前由确定性代码强制实施。

### 5.4 多 Agent 需要以计划和并行为前置

没有结构化任务、依赖关系、取消和冲突控制时，多 Agent 只是多个不受控会话。应先完成 Plan-and-Execute 与有界并行，再引入角色协作。

### 5.5 终端渲染必须后置产品化

早期阶段应使用简单输出验证 Agent 行为。核心模式稳定后再抽象 Renderer 和行内终端，避免 UI 复杂性阻塞主链路。

### 5.6 成熟实现可以迁移，但不能整库一次导入

授权参考工程包含 45 个历史提交、208 个主源码文件和 120 个测试文件，并保留从基础 ReAct 到后续能力的演进线索。直接导入最终状态会带来三个问题：

- 早期阶段无法独立编译，提交历史不能体现依赖演进。
- 包名、配置目录、品牌和 Java 版本耦合广泛，机械替换风险高。
- 维护者容易只看到最终结构，无法解释每个阶段解决的问题。

因此采用“历史提交考古 + 选择性迁移 + 当期测试验收”：先从与阶段最接近的历史快照理解最小实现，再从最终实现反向补充已修复的缺陷和边界。

## 6. 产品策略结论

### 6.1 采用编码闭环优先路线

选择纵向闭环而不是模块堆叠：

1. 工程基线与终端对话。
2. ReAct 和本地编码工具。
3. 安全、上下文和精确检索。
4. RAG、计划、并行与 Multi-Agent。
5. 多模型、MCP、Web、Skill 和终端产品化。
6. 诊断、快照、Runtime、多模态与开源发行。

### 6.2 采用选择性迁移而非全量复制

每期实施遵循：

1. 固定参考提交或最终模块范围。
2. 阅读本期源文件、测试和相关文档。
3. 画出依赖与消息流，确认哪些代码属于本期。
4. 迁移最小实现到 `com.xhlcli`。
5. 升级到 Java 21，并把配置目录改为 `.xhlcli` / `~/.xhlcli`。
6. 先迁移并运行行为测试，再做必要重构。
7. 补充 XhlCLI 的差异化需求和回归测试。
8. 更新 README、CHANGELOG 和阶段演示。

### 6.3 不伪造工程历史

- 所有 XhlCLI 提交使用实际开发时间。
- 不导入原仓库 `.git`，不复制原提交作者或时间。
- 不使用一次全量复制后人为拆分提交的方式伪装开发过程。
- 每个提交必须对应一次真实迁移、理解、验证或改进工作。

## 7. 授权与公开发布约束

项目所有者已确认拥有修改和公开再发布授权。开始迁移受版权保护的源文件前，还需把授权要求转化为仓库可执行规则：

- 确认是否需要保留原版权声明或增加 NOTICE。
- 确认 XhlCLI 新增代码采用的许可证。
- 确认第三方依赖许可证兼容性。
- Release 前运行许可证和产物内容检查。

若书面授权包含署名、范围或分发条件，相关声明的准确性优先于品牌文案要求。

## 8. 技术基线结论

- 语言与运行时：Java 21。
- 构建：Maven Wrapper + Maven。
- 架构：单进程、终端优先、本地优先。
- 模型接入：HTTP 流式接口，Provider 适配层。
- 持久化：早期文件，后期按场景使用 SQLite。
- 并发：Java 21 有界执行器，优先虚拟线程但不依赖 Preview API。
- 测试：JUnit 5、Mock HTTP Server、Fake Model、临时目录与临时 Git 仓库。
- 分发：可执行 Fat JAR，后期补充跨平台安装。

## 9. 待验证假设

| 假设 | 验证阶段 | 判断标准 |
|---|---|---|
| Java CLI 用户接受环境变量配置模型 | Phase 01 | 新用户 10 分钟内完成首次对话 |
| ReAct 可稳定完成小型代码任务 | Phase 02–03 | 基准任务无需手工编辑且测试通过 |
| 精确搜索足以覆盖多数日常定位 | Phase 06 | Golden Set 命中率与 RAG 调用占比 |
| 用户愿意为高风险操作审批 | Phase 04 | 安全拒绝正确且审批次数可接受 |
| 上下文压缩能维持长任务目标 | Phase 05 | 压缩后关键状态保留率 |
| 多 Agent 的质量收益高于额外成本 | Phase 10 | 与单 Agent 的成功率、Token、耗时对比 |
| 行内终端比全屏界面更适合编码流 | Phase 15 | 手工矩阵和使用反馈 |

## 10. 研究输出

- 产品范围：[`PRD.md`](PRD.md)
- 技术方案：[`TECH_DESIGN.md`](TECH_DESIGN.md)
- AI 开发规则：[`AGENTS.md`](AGENTS.md)
- 参考实现迁移地图：[`docs/engineering/source-adoption-map.md`](docs/engineering/source-adoption-map.md)
- Phase 00 实施计划：[`docs/plans/2026-08-25-phase-00-project-foundation.md`](docs/plans/2026-08-25-phase-00-project-foundation.md)

## 11. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-25 | 完成立项研究、选择性迁移策略和待验证假设 |
